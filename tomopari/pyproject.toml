[build-system]
requires = ["setuptools>=42.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
dependencies = [
    "torch"
]

[tool.black]
line-length = 79

[tool.isort]
profile = "black"
line_length = 79
